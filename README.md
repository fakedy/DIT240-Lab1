HOW TO RUN

To run http server run http_server.go in a terminal and specify the port number that the webserver will use.

To run the proxy sever run proxy_server.go in a terminal specify the port number that the proxy server will use.


